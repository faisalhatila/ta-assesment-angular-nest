export const environment = {
  production: true,
  /** Public Nest API (same host or full URL). Set in `environment.prod.ts` for deployment. */
  apiBaseUrl: '/api',
  supabaseUrl: '',
  supabaseAnonKey: '',
};
